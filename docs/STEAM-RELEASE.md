# Steam配信 タスクリスト（2026-08-19 起票・2026-09-02 再棚卸し）

ブラウザゲームとして GitHub Pages で公開している本作を、Steam で有料配信するまでの作業一覧。
**Pages 版は残す**（バイラル導線として機能しているし、`tools/shot.mjs` の撮影ハーネスが依存している）。
デスクトップ版はラッパを別ディレクトリに足す形で作り、`index.html` / `src/` / `vendor/` は共有する。

## 0. 決定事項（2026-08-19・ユーザー判断）

| 項目 | 決定 |
|---|---|
| 価格 | 低価格（¥100前後＝Steam の下限帯）。**ただし内容を増強してから出す** |
| 対応プラットフォーム | Windows ＋ Steam Deck（Proton 経由） |
| Steamworks 連携 | 実績 ＋ Steam Cloud まで |
| 言語 | 日本語 ＋ 英語（UI・ストアページとも） |
| 目標時期 | 確定申告シーズン＝**2027年2月**（毎年再燃する季節性コンテンツなので、初年度を外しても翌年がある） |

## 0.5 現在地（2026-09-02 再棚卸し）

起票から2週間で、**「ブラウザ版に足りないもの」の側はほぼ片付いた**。
残っているのは「Electron の外殻」「Steamworks」「ストア資産」「事務」の4つで、
**この4つは互いにほとんど依存していない＝並行して進められる**。

| フェーズ | 状態 |
|---|---|
| P0 検証 | **未着手**。Electron spike / 実GPU FPS / 円の下限価格 |
| P1 事務 | **未着手**。$100 の支払いで30日タイマーが回る。ここが最長のリードタイム |
| P2 デスクトップ化 | **半分完了**。P2-4 / P2-7 / P2-9 済。`desktop/` はまだ存在しない |
| P3 Steamworks | **未着手**。P3-2（実績）は配線するだけなので費用対効果が最大 |
| P4 Deck / パッド | **未着手**。`Gamepad` API の参照がコードに1つも無い |
| P5 ビルド | **未着手**。バージョン定数がコードに存在しない（P5-4） |
| P6 ストア資産 | **未着手**。実作業量が最大 |
| P7 法務 | **P7-1 済**。残りは判断だけ |
| P8 内容増強 | **P8-1 済**。8/19以降の作業はすべて P8-2（怪人のモデル強化）に入っている |
| P9 QA | 未着手（当然） |

### 8/19 以降に完了した項目

- **P2-7** 日本語フォント同梱（Proton 対策）— 2026-08-28
- **P2-9** UI 文言の5言語化 — 2026-08-28
- **P2-4** ポーズ／設定メニュー（音量・感度・明るさ・画質・言語）— 2026-08-28
- **P7-1** ゲーム内クレジット画面 — 2026-08-28
- **P8-1** 英語版の異変再設計（16異変 × 5ロケール）— 2026-08-28

いずれもテスト付き（`npm test` = 172 assertions / 47 suites、2026-09-02 時点で全通過）。

### この2週間で進んでいない項目

**8/19 以降のコミットは `src/game.js` と `docs/HANDOFF.md` と `tools/` にしか入っていない**（怪人の造形＝P8-2）。
P8-2 は §11 の整理では「削れる項目」に分類されている。
**出荷に必要な作業は、この期間に1つも進んでいない。** 造形を続けるかどうかは判断だが、
P1（事務）だけは技術と完全に独立しているので、**造形と並行して今日でも着手できる**。

## 1. この決定が効いてくる点（作業前に読む）

### ¥100 は返金リスクを実質無効化する

Steam の返金は「プレイ2時間未満かつ購入から2週間以内」なら原則通る。1周30分級の本作を通常価格で出すと**全額返金され得る**のが最大の事業リスクだったが、¥100 なら返金1件の損失が¥100なので**値付けの側で既に解決している**。

→ **したがって「内容増強」は返金対策ではなく、レビュー点数と口コミのための投資として扱う。**
間に合わなければ削れる項目であり、ローンチを遅らせる理由にはしない。この整理を忘れると
「2時間耐性をつけるまで出せない」という存在しない制約に縛られる。

### ただし下限価格には副作用が3つある

- **最大割引率が 50% に制限される。** Steam の最低取引価格は基準価格の約半分（$0.49 USD 相当）。$0.99 tier は 50%、$1.99 tier は 75%、$4.99 tier は 90% まで。セール施策の幅が最も狭い帯を選んでいることになる
- **発売後30日は価格変更ができない。** 値上げすると、そこから30日間は割引を出せない
- **通貨ごとに価格を入れないとその国で買えない。** 円を空欄にすると日本で購入不可。37通貨＋4地域グループ全てに入れる
- 下限は「$0.99 USD tier を Steam の Multi-Variable Conversion で各通貨に換算した額」。**円の確定値は Steamworks のログイン後ページで確認すること**（公開ドキュメントには載っていない）。¥100 は下限にほぼ張り付く水準なので、換算結果が¥100を上回るなら弾かれる

出典: <https://partner.steamgames.com/doc/store/pricing>, <https://partner.steamgames.com/doc/store/pricing/currencies>

### 英語化の作業量は「量」ではなく「異変の設計」

**実測（2026-08-19）: 日本語を含む文字列リテラルは 178本 / 約3,900文字**（`src/*.js` ＋ `index.html`、コメントを除外して計測）。英語で2,000語程度で、翻訳そのものは小さい。難所は別のところにある。

- **文字の差し替えが異変の一種である。** 異変プール16種のうち、日本語の見た目に依存するもの（漢字の一部が違う・年号がずれる・書式が微妙に違う）は**英訳すると気づきが消える**。英語版は「同じ難易度の別の異変」に置き換える設計作業が要る。ここは翻訳ではなく仕様
- **Canvas 描画のレイアウトが日本語前提。** 書類・ポスターは Canvas に `fillText` で描いている。行折り・字送り・フォントサイズが日本語の字幅前提なので、英語では溢れる／余る
- **テーマの異国性は売りにできる。** e-Tax・マイナンバーカードは日本固有だが、「締切前夜の税務書類」自体は普遍。ストア説明文は「日本の確定申告」という設定をそのまま前に出す方が、無理にローカライズするより刺さる（HANDOFF の固有名詞方針＝パロディ寄せとも整合させる）

### Proton 上では日本語が豆腐になる

Windows ネイティブなら Meiryo / Yu Gothic に暗黙依存できるが、**Proton の prefix には日本語フォントが入っていないことが多い**。Steam Deck 対応を選んだので、**フォントを同梱して `@font-face` で明示的に読むのが必須作業**になる。再配布可のもの（Noto Sans JP / M PLUS 等 OFL）を選び、`THIRD-PARTY-NOTICES.md` に追記する。

### Steam オーバーレイは乗らない可能性がある

Valve は「オーバーレイは常時全画面のフレーム提示を要求する。dirty rect で部分描画するブラウザのモデルとは合わない」と明記し、回避策として「native アプリに offscreen Chromium を埋めて毎フレーム描く（容易ではない）」を挙げている。一方 `steamworks.js` は Electron 用に `electronEnableSteamOverlay()` を提供している。**P0-1 の spike で実測し、乗らなければオーバーレイ非対応で出す**（致命ではない）。ソフトウェアラスタライズでは動かないとも明記されているので、実GPU で確認すること。

出典: <https://partner.steamgames.com/doc/features/overlay>, <https://github.com/ceifa/steamworks.js>

## 2. クリティカルパス（事務が最長・技術と並行できる）

| 制約 | 期間 | 起点 |
|---|---|---|
| Steam Direct $100 支払い → 発売可能まで | **30日** | 支払日 |
| 「近日登場」ストアページ公開 → 発売まで | **2週間以上** | ページ公開日 |
| ビルド／ストアレビュー | 1〜5日 | 提出日 |
| 価格変更の凍結 | 発売後30日 | 発売日 |

出典: <https://partner.steamgames.com/steamdirect>

**2027年2月中旬の発売から逆算すると、遅くとも2026年12月末までに P1（事務）を着手・完了しておく。**
技術が完成しても事務が終わっていなければ出せないので、**P1 は P0 より先に着手してよい**（並行）。

## 3. フェーズ0: 検証（後段の前提を確定させる）

- **P0-1 ラッパ技術の spike（1日）** — Electron を第一候補として実測する。①`vendor/three` の ES modules ＋ importmap がそのまま動くか ②実GPU での WebGL2 ＋ポストプロセス（GTAO / Bloom / 自作 filmPass）の FPS ③Steam オーバーレイが乗るか。Tauri は WebView2 依存でオーバーレイ・GPU挙動がさらに不利、NW.js は保守状況が弱い。**判断基準を先に決めておく**（オーバーレイは「乗れば得、乗らなくても出す」）
- **P0-2 実GPU での FPS 計測** — HANDOFF 6章の残TODO。画質プリセット（P2-4）とシステム要件（P6-5）の共通の前提。重い場合の調整枠は HANDOFF 6章に既に列挙してある（bloom 解像度・`shadow.mapSize` 1024→512・小物の `seg`）
- **P0-3 円の下限価格の確認** — Steamworks ログイン後の minimums ページで、¥100 が下限を満たすか確認する。満たさなければ ¥110〜¥120 等へ

## 4. フェーズ1: 事務（今すぐ着手）

- **P1-1** Steamworks パートナー登録（個人／法人の選択）・身元確認
- **P1-2** **Steam Direct $100 を支払う**（1本あたり。$1,000 USD の Adjusted Gross Revenue 到達後の支払いで回収される）→ **30日タイマーが回り始める**
- **P1-3** 税務質問票（日本在住の個人なら W-8BEN 相当）＋銀行口座登録。**口座名義が身分証の名義と一致していること**が要件
- **P1-4** アプリ作成、コンテンツ調査（ホラー表現・暴力表現の申告）、年齢レーティング設問
- **P1-5** 「近日登場」ストアページ公開（**発売2週間以上前**）。P6 の資産が揃ってから
- **P1-6** 37通貨＋4地域グループの価格入力（円必須。抜けるとその国で買えない）

## 5. フェーズ2: デスクトップ化

- **P2-1** `desktop/` に Electron main を置き `index.html` をロード。asar 同梱、devtools／コンテキストメニュー無効、外部ナビゲーション禁止、CSP
- **P2-2** 表示: フルスクリーン既定・ボーダレス／ウィンドウ切替・レンダースケール・DPI・複数モニタ。現状 `innerWidth/innerHeight` 直参照が多数（`src/game.js:265-359`）なのでリサイズ経路は既にある
- **P2-3** 入力: ポインタロックの取り回し（Esc → 解除してポーズへ）、キーコンフィグ、マウス感度。**タッチUIをデスクトップで無効化**（`src/game.js:2312` 〜, `index.html:156` 〜）
- ~~**P2-4** **ポーズ／設定メニューを新規実装** — 音量（`audio.js` の設定を流用）、マウス感度、画質プリセット、明るさ。~~ **済（2026-08-28）**<br>
  　`index.html:374-417` の `#pause` に 音量 `#pVol` / 感度 `#pSens` / 明るさ `#pGamma` / 画質 `#pQual`（auto・low・high）/ 言語 `#pLang` の5列。値は `save`（`src/game.js:179-190`）に永続化。<br>
  　⚠️ **明るさの既定値は動かさない。** HANDOFF 6章に「v9 の値は実機で承認された完成値」と記録がある。既定は `gamma: 125`（＝1.25）のまま。プレイヤー側の調整幅として足してある。<br>
  　**残るのは Electron 側の項目だけ**：ウィンドウモード切替（P2-2）とキーコンフィグ（P2-3）。この2つはブラウザ版に置き場所が無いので P2-1 の後になる
- **P2-5** セーブを `localStorage`（`src/game.js:140,147`）から `userData` 配下の JSON へ移行。破損時フォールバック込み。Steam Cloud（P3-3）の前提
- **P2-6** 多重起動防止・クラッシュログ・終了処理
- ~~**P2-7** 日本語フォント同梱と `@font-face` 明示（**Proton 対策・Deck 対応の必須項目**）。`THIRD-PARTY-NOTICES.md` に追記~~ **済（2026-08-28）**<br>　Noto をサブセットして 7 face・合計 0.69MB を同梱。ja/en/ru/es は JP 系、zh-Hans は SC 系。<br>　着手前は `document.fonts` が空＝OS フォント頼みだった（Proton では全文が豆腐）。<br>　仕様と実測: `docs/test-specs/font-bundling.md`／テスト: `tests/unit/fonts.test.js`（F-01〜F-12）<br>　再生成: `node tools/charset.mjs && node tools/make-fonts.mjs`（開発機のみ。生成物はコミット済み）
- **P2-9** UI 文言の多言語化（**済 2026-08-28**）<br>　HUD・ポーズ・タイトル・演出文・アイテム説明・暗証番号 UI・結末・ランク・3D看板を5言語化。<br>　着手前は暗証番号の手掛かり3つがすべて日本語のみで、**日本語以外では詰んで進めなかった**。<br>　クレジット（`#credits`）はライセンス表記なので日本語のまま（利用者の指示）。<br>　仕様と実測: `docs/test-specs/ui-i18n.md`／テスト: `tests/unit/ui.test.js`（U-01〜U-16）<br>　副次効果: 同梱フォントが 0.69MB → 0.47MB
- **P2-8** 配布物の削減。**2026-09-02 に実測した（削減量が判明したので、以下の数値で作業できる）**

  | 対象 | 現状 | 参照あり | 削れる |
  |---|---|---|---|
  | `assets/textures` | 26.9MB | **4.3MB** | **22.6MB（84%）** |
  | `vendor/three/addons`（115ファイル） | 837KB | 153KB（17ファイル） | 684KB |

  参照の判定根拠: `loadPBRSet()`（`src/game.js:801`）が実際に呼ばれている base は
  `cardboard001 / concrete034 / fabric049 / leather030 / metal063 / plastic011`（`.webp`）と
  `plaster017 / fabric001`（`.jpg`）の8つだけ。加えて個別参照が
  `hardwood2_diffuse.jpg` / `hardwood2_bump.webp` の2枚。**残りは全部使っていない。**

  削除対象の内訳:
  - **webp 化済みの jpg 原本**（`cardboard001 / concrete034 / fabric049 / leather030 / metal063 / plastic011` の `_diffuse|_normal|_roughness.jpg`）— 12.7MB。同名 webp が現役なので二重持ち
  - **v22 で参照を切った写真テクスチャ** — `doc_{memo,myna,receipt,tax}_base.jpg`（3.6MB）、`poster_{normal,anom}.jpg`（1.6MB）、`stamp_curse.jpg`（0.9MB）、`monster_face.jpg`（1.0MB）
  - **和室時代の名残** — `tatami005_*`（0.7MB）、`hardwood2_roughness.jpg`（0.1MB。HANDOFF 7章に「意図的に外している」と記録あり）
  - **未使用 addon 107ファイル** — 到達可能なのは EffectComposer / RenderPass / UnrealBloomPass / GTAOPass / ShaderPass / OutputPass / RoomEnvironment / RoundedBoxGeometry / BufferGeometryUtils とその推移的依存の計17ファイルのみ

  ⚠️ **`git rm` ではなく「配布物に含めない」で解く。** 写真テクスチャは造形の突き合わせ（HANDOFF 13章）で戻す可能性があり、リポジトリからは消さない方がいい。P5-1 のビルド設定（electron-builder の `files` 除外パターン）側で落とすこと

- **P2-10 `window.__dbg` を製品ビルドで塞ぐ**（2026-09-02 に発見・新規） — `src/game.js:6231` がセーブ・異変プール・`ending()` ・`startOmen()` をグローバルに晒している。開発中は必須だが、**Steam 版ではネタバレと実績の不正解除の経路になる**（asar は難読化ではないので「読めない」前提には立てない）。ビルド時フラグで剥がすか、`__dbg` が生えているセーブに実績を付けない。P3-2 の前提
- **P2-11 `tools/` と `tests/` を配布物から外す** — `.mjs` の撮影ハーネス群と `docs/` は製品に不要。P5-1 の `files` 指定で一緒に落とす（P2-8 と同じ作業なのでまとめてやる）

## 6. フェーズ3: Steamworks 連携

- **P3-1** `steamworks.js` を組み込む（`greenworks` は作者いわく非保守）。native module なので renderer から直接触らず **main 経由の IPC** にする（README の `contextIsolation: false` はセキュリティを落とすので避ける）。製品ビルドに `steam_appid.txt` を混ぜない
- **P3-2** **実績設計** — 4エンディング／異変図鑑16種／判定ランクS／青色申告解禁 が既に localStorage に永続化されている。**ほぼ配線するだけで実績になる**ので費用対効果が最も高い
- **P3-3** Steam Cloud 設定（P2-5 でセーブが1ファイルになっていれば設定のみ）
- **P3-4** オーバーレイ有効化（P0-1 の結果次第）

## 7. フェーズ4: Steam Deck / コントローラ

- **P4-1** ゲームパッド操作（Steam Input）: 移動・視点・E相当。**現状 `Gamepad` API の参照はコード内に1つも無い**（2026-09-02 確認）＝完全な新規実装。さらに**書類検分UI・ポーズ・e-Tax の暗証番号入力がすべてマウスクリック／キーボード前提**なので、フォーカス移動と決定・戻るの配線がフェーズ4の実体。⚠️ 暗証番号は `<input type="text">`（`index.html:481`）なので、パッド単独では**入力手段が存在しない**。オンスクリーンキーパッドかステアリング入力が要る
- **P4-2** 1280×800 での可読性 — **本作は文字を読ませるゲーム**（異変の一つが文字差し替え）なので、HUD・書類・字幕のスケーリングは体験に直結する
- **P4-3** Proton での通し確認（Windows ビルドを Proton で走らせる。Linux ネイティブビルドは作らない）
- **P4-4** Deck 互換性の申請（Verified / Playable のバッジは露出に効く）

## 8. フェーズ5: ビルドとアップロード

- **P5-1** electron-builder（or forge）の選定。Windows コード署名は Steam 経由配布なら未署名でも実害が小さい（EV証明書の年額に対して効果が薄い）
- **P5-2** **SteamPipe**: depot 構成、`app_build` / `depot_build` の vdf、launch options、`steamcmd` でのアップロード。手動運用で始め、頻度が上がったら CI へ
- **P5-3** default / beta ブランチ運用、レビュー用ビルド提出（1〜5日）
- **P5-4** バージョン表記の一元化 — **コード側にバージョン定数が1つも無い**（2026-09-02 確認）。`vNN` は README の変更履歴と `src/game.js` のコメント内にしか無く、実行中のビルドが何版かを画面から確認する手段がゼロ。`package.json` の `version` を単一の出所にして、Electron の `app.getVersion()` → タイトル画面の `#meta` とクラッシュログに流す。**バグ報告が「どの版か分からない」状態で来るのを防ぐための最低限**

## 9. フェーズ6: ストアページ資産（実作業量が最大）

カプセルの寸法は 2026-08-19 時点の公開ドキュメント実測値。出典: <https://partner.steamgames.com/doc/store/assets/standard>

| 資産 | 寸法 |
|---|---|
| ヘッダーカプセル | 920 × 430 |
| スモールカプセル | 462 × 174 |
| メインカプセル | 1232 × 706 |
| 縦型カプセル | 748 × 896 |
| ページ背景（任意） | 1438 × 810 |
| スクリーンショット | 1920 × 1080 以上・16:9・**最低5枚** |

ライブラリ資産（ライブラリカプセル／ヒーロー／ロゴ）は別ページの仕様。

- **P6-1** カプセル4種。**カプセルにはタイトルロゴタイプが要る**。⚠️ `agy` は文字が崩れるので画像生成に頼れない（HANDOFF 15章の実測）。Canvas か手作業になる
- **P6-2** スクリーンショット。`tools/shot.mjs` のアングル定義がそのまま使える。**全年齢適合フラグを4枚以上に付けないと露出が落ちる**（トップページのホバーアートから除外される）
- **P6-3** トレーラー。実況・切り抜き導線を狙う企画なので優先度は高い
- **P6-4** 説明文・短い説明・タグ（日英）
- **P6-5** システム要件（P0-2 の計測結果が必要）
- **P6-6** 商標・実在ロゴを使わない確認（HANDOFF の権利方針を踏襲）

## 10. フェーズ7: 法務

- ~~**P7-1** **ゲーム内クレジット画面を実装し `THIRD-PARTY-NOTICES.md` の内容を載せる。**~~ **済（2026-08-28）**<br>
  　`index.html:420-462` の `#credits`（ポーズ → クレジット）。three.js / three.js examples 由来テクスチャ / Noto サブセット / ambientCG を記載。<br>
  　⚠️ **P2-8 でテクスチャを削ったら、このリストと `THIRD-PARTY-NOTICES.md` の両方を同時に直すこと**（片方だけ直すと不整合になる。`index.html:418` にも同じ注意書きあり）。<br>
  　⚠️ ライセンス全文の同梱は**まだ配布物側の宿題**。クレジット画面は `vendor/three/LICENSE` と `assets/fonts/OFL.txt` が「配布物内にある」と書いているので、**P2-8 の除外パターンでこの2つを消してはいけない**
- **P7-2** `LICENSE` を置かない方針は維持（HANDOFF の権利まわり参照）。ただし **asar は難読化ではない**ので、ソースは実質読める前提で臨む
- **P7-3** 実名（国税庁 / e-Tax / マイナンバーカード）の扱いを商用化前に再判断。パロディ寄せ方針（「ヨドバチカメラ」）と一貫させるか
- **P7-4** ホラー表現の注意書き（点滅・大音量）。P1-4 のコンテンツ調査と整合させる
- **P7-5** 外部送信を足さない（足さなければプライバシーポリシーが不要になる）

## 11. フェーズ8: 内容増強（優先度順・削れる項目）

¥100 という値付けにより**返金対策としては不要**。レビュー点数と口コミのための投資。既存TODOから、費用対効果の高い順：

- ~~**P8-1** 英語版の異変再設計（言語決定に伴う必須作業。ここは削れない）~~ **済（2026-08-28）**<br>
  　§14 の設計どおり、異変16種を5ロケール分実装した（`src/anoms.js`）。忌み数（`UNLUCKY`: ja/zh-Hans は 4、en/ru/es は 13）、
  　混同文字表（`CONFUSABLES`）、氏名とフォールバック手掛かり（`playerName` / `playerNameAlt`）、当局名、日付書式を
  　ロケールごとに持つ。仕様: `docs/test-specs/anoms-i18n.md`／テスト: `tests/unit/anoms.test.js`
- **P8-2** 怪人「カクシン様」のモデル強化 — 部屋の作り込みに対して**相対的に一番の粗**。手法の当てもある（Icosahedron → 単位球座標でノイズ変位 → `computeVertexNormals`）。⚠️ ホッケーマスク方向へ戻さない（HANDOFF 権利まわり）
- **P8-3** 異変プールの追加（現状16種。図鑑の埋まり方が周回動機なので、実績と直結する）
- **P8-4** 隠れ場所の追加（現状クローゼットのみ）
- **P8-5** 足音のステレオ方向感（現状モノラルのビープ）

## 12. フェーズ9: 出す前のQA

- **P9-1** 素の Windows で Steam クライアント経由のインストール〜1周クリア
- **P9-2** 別解像度・ノートPCの内蔵GPU
- **P9-3** セーブの永続と削除、Cloud の同期
- **P9-4** **実績の発火を全件確認**（取りこぼしが多い）
- **P9-5** Steam オフラインモードでの起動
- **P9-6** Steam Deck 実機（または Proton）で日本語・英語の両方が豆腐にならないこと

## 13. 直近3タスク（2026-09-02 更新）

起票時と変わっていない。**2週間で1つも消化されていないので、そのまま再掲する。**

1. **P1-1 / P1-2** — Steamworks 登録と $100 支払い。30日タイマーを回す。技術と完全に並行できる。
   2027年2月の発売から逆算した P1 の締切は**2026年12月末**なので、まだ余裕はある。
   ただし身元確認と税務質問票（P1-3）は書類の往復があり、自分の手だけでは短縮できない
2. **P0-1** — Electron spike（1日）。**判断基準を先に決めてから触る**。見るのは
   ①`vendor/three` の importmap が `file://` で解決するか（`index.html:514` の相対パス指定なので通るはずだが、実測する）
   ②実GPU での WebGL2 ＋ GTAO / Bloom の FPS ③Steam オーバーレイ。
   オーバーレイは「乗れば得、乗らなくても出す」
3. **P0-2** — 実GPU FPS 計測。画質オプション（P2-4 の `#pQual` は auto/low/high の枠だけ既にある）と
   システム要件（P6-5）の両方の前提。**HANDOFF 6章の残TODO でもあり、8/17 から持ち越されている**

### 「パッケージング」だけに絞るなら、最短経路は P0-1 → P2-1 → P5-1 → P2-8

Steam に出す全体ではなく **「配布できる .exe を作る」** ところまでなら、必要なのは
P0-1（spike）→ P2-1（Electron main）→ P5-1（electron-builder）→ P2-8 / P2-11（除外パターン）の4つ。
P2-5（セーブの移行）は `localStorage` が Electron でも動くので**出すだけなら不要**だが、
**P3-3（Steam Cloud）の前提なので、実績と Cloud を入れるなら先にやる**。

---

## 14. ローカライズ設計（2026-08-19 調査）

### 14.0 タイトル確定（2026-08-19 決定）

ストア名・カプセルのロゴタイプ（P6-1）・実績名はこの文字列を使う。
経緯と根拠は `docs/test-specs/anoms-i18n.md` §8（L-23 / L-24）。

| ロケール | タイトル |
|---|---|
| `ja` | カクシン様 ─ 確定申告からは逃げられない |
| `en` | KAKUSHIN — No Escape from Your Tax Return |
| `zh-Hans` | KAKUSHIN — 逃不掉的个税汇算 |
| `ru` | KAKUSHIN — От налоговой не убежать |
| `es` | KAKUSHIN — De Hacienda no se escapa |

- **怪異の名前を先頭に置く。** Steam はタイトルを 1 フィールドしか持たず、一覧や検索結果で
  末尾から切り詰められる。名前が末尾にあると、切り詰めとカプセルのロゴタイプの両方から落ちる。
- **非日本語圏はラテン文字 `KAKUSHIN` で固定**（L-24）。`确信` は quèxìn と読まれ、
  音でブランドが繋がらないため使わない。
- **これは公平性の修正でもある。** 変更前は `カクシン` がプレイヤーに一度も提示されておらず、
  異変 `issuer`（発行元＝株式会社カクシン）を却下する根拠がゲーム内に存在しなかった。
- P6-1 のロゴタイプは `KAKUSHIN` の 8 文字だけ描ければ全非日本語圏で使い回せる
  （`agy` は文字が崩れるので手作業だが、対象が 8 文字なら現実的）。


### 14.1 内容は国別に置き換える（2026-08-19 決定）

翻訳ではなく**国別コンテンツ**にする。書類・暦・氏名・忌み数・暗証番号・当局名を
その国の実物に置き換える。設計と実装記録は `docs/test-specs/anoms-i18n.md` §9。
下の調査表は、その決定の土台になった一次調査である。

### 確定申告という概念は主要国すべてにある。ただし「怖さの形」が違う

| 国 | 制度 | 締切 | 誰がやるか | 本作との相性 |
|---|---|---|---|---|
| **英国** | Self Assessment（HMRC） | **1月31日 23:59**・オンライン | 自営業・副業者 | ◎ **構造が完全一致**（夜の時限・オンライン送信・遅延で即 £100） |
| **米国** | Form 1040（IRS）"Tax Day" | 4月15日 | **給与所得者も全員**（年末調整が無い） | ◎ 母集団が日本より広い。IRS監査の恐怖は既に定番の題材 |
| ドイツ | Steuererklärung（ELSTER） | 原則7月31日（近年延長続き） | 会社員も還付狙いで任意申告 | ○ Finanzamt への畏れが強い |
| 中国 | 综合所得**年度汇算**（2019年改正・2020年初実施） | **3月1日〜6月30日**・个人所得税アプリ | 総合所得のある居住者 | ◎ 〔2026-08-19 上方修正〕**窓の最終日 6/30 23:59 は一点**であり、駆け込みも社会現象として実在する。「締切前夜」の枠はそのまま成立する（`docs/test-specs/anoms-i18n.md` L-26） |
| 韓国 | 연말정산（「13月の給料」） | 1〜2月 | 会社員 | ○ 国民的行事 |
| 北欧 | 事前記入済み・SMS承認レベル | — | — | ✕ **恐怖が成立しない** |

出典: <https://www.gov.uk/self-assessment-tax-returns/deadlines>（オンライン締切 23:59 を実測確認）、
<https://taxsummaries.pwc.com/peoples-republic-of-china/individual/tax-administration>（年度汇算 3/1〜6/30）

### 方針：制度を差し替えず、日本設定のまま翻訳する

参照点である8番出口や Chilla's Art は**日本設定のまま英語字幕だけ付けて**海外実況で跳ねている。
異国性は障壁ではなく売りであり、制度を国別に差し替えると開発コストが数倍になった上にその武器を捨てる。
やるのは「**忠実な翻訳＋公平性の担保**」。

**公平性の担保＝異変は「現実の日本の税制知識」ではなく「ゲーム内で提示された情報との矛盾」で
気づけること。** これは v21 で暗証番号に対して既に適用した原則（必須アイテムだけで解ける）の再適用。

### 異変16種の棚卸し（`src/game.js:84` の `ANOMS` 全件）

**12種は翻訳だけで生き残る**: `minus` 負の金額 / `stamp` 逆さの印 / `mirror` 鏡文字 /
`issuer` 実在しない発行元 / `date` 2月30日 / `soul` 魂 / `kami` 名乗る書類 / `eye` 見ている印 /
`blur` 濡れた文字 / `mark` 透かしの顔 / `ju` 朱の呪 / `typo` 題字入れ替え。

**4種は英語版で作り直しが必要**:

| 異変 | 問題 | 対応案 |
|---|---|---|
| `era` 存在しない年号 | 「令和∞年」は∞が数値でないので残るが、「**昭和107年**」は昭和が64年で終わったことを知らないと気づけない | 英語版は在り得ない年（未来年・4桁でない年）へ差し替え |
| `name` 一字ちがいの氏名 | 三月 十五 → 三月 十**六**。漢字が読めない前提では差分が見えない | ラテン文字で1文字違いを作る（下記の氏名決定と連動） |
| `four` 四づくし | 4＝死。**中国語圏では完璧に通じるが英語圏では「変だな」で止まる** | 英語版は 6 の連続（666）等、西洋の忌み数へ差し替え |
| `label` 化けた項目名 | `LOOKA` 表（金→全, 医→圧, 番→蕃）は**漢字の字形**の話 | **ラテン文字に等価物がある。** `rn`/`m`, `l`/`I`, `0`/`O`, `vv`/`w` →「Amount → Arnount」「Total → TotaI」。フィッシングの実手法なので不気味さも付いてくる。**英語版の方が良くなる可能性がある** |

### 暗証番号パズルは日付書式に依存している（要対応）

答えは `0315`（MMDD）。**MMDD は米国式で、英国・欧州は 15/03 と書くため `1503` を試す。**
さらに `PLAYER_NAME = "三月 十五"` は単なる氏名ではなく**パズルのフォールバック手掛かり**
（`src/game.js:2662` で「氏名欄には、いつもの名前があった」と再提示している）。
**氏名を訳すとパズルの救済経路が壊れる。**

→ **英語版の氏名案: "Ides March"。** Ides of March = 3月15日で、"Beware the Ides of March"
（シーザー暗殺日）が付いてくるので恐怖の側にも効く。日付書式は英語版の手掛かり文で
「month first, then day」と明示する。

### 実装上の含意

異変の `apply()` は日本語の文字列操作そのもの（`LOOKA` 表、`[t[0], t[1]]` の入れ替え、
漢字前提の正規表現）。**単に文字列を外部ファイルへ抽出するだけでは英語版にならない。**
異変を「言語ごとの実装」として分離する構造変更が要る。i18n 基盤の設計は
文字列テーブルではなく**異変の側から**決めること。
